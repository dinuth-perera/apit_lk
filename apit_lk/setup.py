# setup.py
from setuptools import setup, find_packages

setup(
    name='apit_lk',
    version='0.1',
    packages=find_packages(),
    install_requires=[],
    tests_require=['unittest'],
    test_suite='tests',
)
